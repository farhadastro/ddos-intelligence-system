services:
  - type: web
    name: ddos-backend
    runtime: python
    rootDir: ddos-intelligence-system/backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api.server:app --host 0.0.0.0 --port 10000
    plan: free
